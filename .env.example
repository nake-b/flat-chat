POSTGRES_USER=flat_chat
POSTGRES_PASSWORD=flat_chat
POSTGRES_DB=flat_chat
DATABASE_URL=postgresql://flat_chat:flat_chat@postgres:5432/flat_chat

# Tailscale sidecar (host machine only — teammates leave this section unset).
# When you opt in by setting COMPOSE_FILE below, `docker compose up` auto-loads
# docker-compose.tailnet.yml, which adds the tailscale service and routes
# Postgres through it. Teammates reach the DB at flat-chat-db.<tailnet>.ts.net:5432.
#
# Generate TS_AUTHKEY at https://login.tailscale.com/admin/settings/keys —
# reusable, non-ephemeral, tagged tag:flat-chat-db.
# TS_AUTHKEY=tskey-auth-<your-key>
# COMPOSE_FILE=docker-compose.yml:docker-compose.tailnet.yml

# LLM — Azure OpenAI (classic Azure OpenAI Service).
# DEPLOYMENT is the name you typed when creating the deployment in Foundry.
# API_VERSION must be a preview version for o-series reasoning models.
AZURE_OPENAI_API_KEY=<your-key>
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=o4-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Jina Embeddings (optional — leave empty to disable semantic search)
JINA_API_KEY=<your-key>
JINA_BASE_URL=https://api.jina.ai/v1

# Observability (dev only; Phoenix UI at http://localhost:6006)
PHOENIX_ENABLED=true
PHOENIX_ENDPOINT=http://phoenix:6006/v1/traces
